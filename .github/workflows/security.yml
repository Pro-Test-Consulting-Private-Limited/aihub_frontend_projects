name: Security Scan

on:
  push:
    branches: ["main", "dev"]
  pull_request:
  schedule:
    - cron: "30 3 * * 1-5"

jobs:
  security:
    uses: Pro-Test-Consulting-Private-Limited/security-workflow/.github/workflows/security-scan.yml@main
